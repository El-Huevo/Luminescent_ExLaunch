#pragma once

enum Rank : int32_t {
    RANK_1,
    RANK_2,
    RANK_3,
    RANK_4,
    RANK_5,
    RANK_6,
    RANK_7,
    RANK_8,
    RANK_9,
    RANK_10,
    RANK_COUNT
};

enum Group : int32_t {
    GROUP_1,
    GROUP_2,
    GROUP_3,
    GROUP_4,
    GROUP_COUNT
};

extern const char* frontierFolderPath;

